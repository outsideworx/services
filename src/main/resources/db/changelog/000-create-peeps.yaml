databaseChangeLog:
  - changeSet:
      id: 000-create-peeps
      author: services
      comment: Creates the peeps table for gaiapeeps client links
      changes:
        - sql:
            sql: |
              CREATE SEQUENCE IF NOT EXISTS peeps_seq START WITH 1 INCREMENT BY 50;
              CREATE TABLE IF NOT EXISTS peeps (
                id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                link VARCHAR(255),
                title VARCHAR(255)
              );
