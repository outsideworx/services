<configuration>
    <property name="CONSOLE_LOG_PATTERN" value="%logger{36} --- requestId=%X{requestId:-unknown}: %msg%n"/>
    <include resource="org/springframework/boot/logging/logback/base.xml"/>
</configuration>